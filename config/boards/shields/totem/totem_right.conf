# Right half (peripheral) specific settings
# (BT_MAX_CONN/BT_MAX_PAIRED already default to 1 for split peripherals)

# Enhanced peripheral responsiveness
CONFIG_ZMK_SPLIT_BLE_PERIPHERAL_STACK_SIZE=2048